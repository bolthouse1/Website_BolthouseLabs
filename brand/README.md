# Brand source art

Not published. `public/` is copied verbatim into `dist/` and deployed, so
anything dropped there goes live; these files are referenced by no page and
sat in `public/` only because the 2026-08-26 migration moved them wholesale.

| File | What it is |
|---|---|
| `logo-t.png` | Transparent MyBodyPrism wordmark, 1175x309 |
| `Body Prism.png` | Icon master art, 1024x1024 |

The art the site actually uses lives in `public/`: `logo-mark-t.webp` (hero and
header brand mark) and `logo.png` (the Open Graph preview image, kept as PNG
because social scrapers handle WebP inconsistently).
